# Phase B.1 深色模式开发计划

**功能：** 深色主题支持
**优先级：** ⭐⭐⭐ 高
**预计工作量：** 3-4天
**开始时间：** 2026-01-03

---

## 📋 目标

为爬虫管理系统添加深色模式，提升夜间使用体验。

### 核心需求
- ✅ 支持亮色/深色主题切换
- ✅ 主题选择持久化（localStorage）
- ✅ 所有页面完美支持深色模式
- ✅ 平滑的主题切换过渡动画
- ✅ 移动端同样支持

### 技术要点
- 使用CSS变量实现主题切换
- 使用`data-theme`属性控制主题
- 保持Glass Morphism设计风格
- 确保可访问性（对比度符合WCAG标准）

---

## 🎯 实施步骤

### Day 1: 基础设施搭建（2-3小时）

#### 1.1 创建主题配置系统
**文件：** `src/web/vue-app/src/config/theme.js`

**功能：**
- 定义主题列表（light, dark）
- 主题获取/设置函数
- 主题持久化（localStorage）

#### 1.2 创建深色主题CSS变量
**文件：** `src/web/vue-app/src/assets/styles/theme-dark.css`

**内容：**
- 深色背景色系统
- 深色文本色系统
- 深色Glass Morphism效果
- 深色组件样式覆盖

#### 1.3 创建主题切换组件
**文件：** `src/web/vue-app/src/components/common/ThemeToggle.vue`

**功能：**
- 月亮/太阳图标切换
- 点击切换主题
- 显示当前主题状态

**产出：**
- ✅ 主题配置系统可用
- ✅ 深色主题CSS变量定义完成
- ✅ 主题切换组件可工作

---

### Day 2: 集成主题系统（4-5小时）

#### 2.1 更新App.vue
**任务：**
- 导入theme.js配置
- 导入theme-dark.css
- 在mounted时初始化主题
- 监听主题变化

#### 2.2 添加主题切换按钮
**位置：** AppHeader.vue右上角

**样式：**
- 使用月亮🌙/太阳☀️图标
- 圆形按钮，Glass Morphism效果
- 悬停提示"切换深色模式"

#### 2.3 验证基础功能
**测试：**
- [ ] 点击切换按钮，主题立即切换
- [ ] 刷新页面，主题保持不变
- [ ] localStorage正确存储主题选择

**产出：**
- ✅ 主题系统完全集成
- ✅ 主题切换功能正常工作
- ✅ 主题持久化成功

---

### Day 3: 样式优化（全天）

#### 3.1 优化核心组件深色样式

**优先级高的组件：**
1. **GlassCard** - 玻璃态卡片
   - 深色背景：`rgba(255, 255, 255, 0.05)`
   - 深色边框：`rgba(255, 255, 255, 0.1)`

2. **TabNavigation** - 标签导航
   - 深色背景渐变
   - 活跃标签高亮

3. **StatCard** - 统计卡片
   - 深色背景
   - 图标色彩优化

4. **DataTable** - 数据表格
   - 深色表头
   - 行悬停效果
   - 斑马纹优化

#### 3.2 优化所有页面深色样式

**页面清单（10个）：**
- [ ] Dashboard.vue - 仪表盘
- [ ] Realtime.vue - 实时监控
- [ ] Scheduler.vue - 调度器
- [ ] History.vue - 历史查询
- [ ] DataManagement.vue - 数据管理
- [ ] AlertsLuxury.vue - 告警管理
- [ ] Sources.vue - 数据源
- [ ] LotteryConfigs.vue - 彩种配置
- [ ] LogsPro.vue - 系统日志
- [ ] DomainManagement.vue - 域名管理

**优化内容：**
- 检查所有硬编码颜色
- 替换为CSS变量
- 确保深色模式可读性

**产出：**
- ✅ 所有核心组件支持深色模式
- ✅ 所有页面深色显示正常
- ✅ 无可读性问题

---

### Day 4: 测试与完善（4-5小时）

#### 4.1 全面测试

**测试清单：**
- [ ] 亮色模式所有页面正常
- [ ] 深色模式所有页面正常
- [ ] 主题切换过渡流畅
- [ ] 移动端深色模式正常
- [ ] 浏览器缓存后主题保持

**测试方法：**
```javascript
// 浏览器控制台快速切换测试
localStorage.setItem('theme', 'dark');
location.reload();

localStorage.setItem('theme', 'light');
location.reload();
```

#### 4.2 可访问性检查

**WCAG对比度标准：**
- 普通文本：至少4.5:1
- 大文本：至少3:1
- UI组件：至少3:1

**检查工具：**
- Chrome DevTools → Lighthouse → Accessibility
- 目标：保持≥90分

#### 4.3 性能测试

**Lighthouse评分：**
- Performance应保持≥95
- Best Practices应保持100
- Accessibility应保持≥90

#### 4.4 细节优化

**优化项：**
- [ ] 添加主题切换过渡动画
- [ ] 优化图标在深色模式下的显示
- [ ] 确保所有徽章在深色模式下清晰
- [ ] 优化加载状态在深色模式下的显示

**产出：**
- ✅ 所有测试通过
- ✅ 可访问性达标
- ✅ 性能不下降
- ✅ 细节完美

---

## 📐 设计规范

### 深色模式色彩系统

#### 背景色
```css
--bg-primary: #0a0e27;      /* 主背景（深蓝黑） */
--bg-secondary: #0f0f1e;    /* 次级背景 */
--bg-tertiary: #16213e;     /* 第三级背景 */
--bg-elevated: #1a1a2e;     /* 浮起元素背景 */
```

#### 文本色
```css
--text-primary: #ffffff;         /* 主文本（白色） */
--text-secondary: #e0e0e0;       /* 次级文本 */
--text-tertiary: #b0b0b0;        /* 第三级文本 */
--text-disabled: #808080;        /* 禁用文本 */
```

#### 功能色（保持不变，但降低亮度）
```css
--success-color: #10b981;    /* 成功绿 */
--warning-color: #f59e0b;    /* 警告橙 */
--error-color: #ef4444;      /* 错误红 */
--info-color: #3b82f6;       /* 信息蓝 */
```

#### Glass Morphism深色版
```css
--glass-bg: rgba(255, 255, 255, 0.05);       /* 玻璃背景 */
--glass-border: rgba(255, 255, 255, 0.1);    /* 玻璃边框 */
--glass-shadow: 0 8px 32px rgba(0, 0, 0, 0.5); /* 玻璃阴影 */
```

### 对比度要求

| 元素类型 | 最小对比度 | 示例 |
|---------|-----------|------|
| 大标题 | 3:1 | h1, h2 |
| 正文文本 | 4.5:1 | p, span |
| 小文本 | 7:1 | 12px以下 |
| UI组件 | 3:1 | 按钮、输入框 |

---

## 🎨 主题切换动画

### 过渡效果
```css
/* 全局过渡 */
* {
  transition: background-color 0.3s ease,
              color 0.3s ease,
              border-color 0.3s ease;
}

/* 主题切换动画 */
[data-theme] {
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}
```

### 切换按钮动画
```css
.theme-toggle {
  transform: rotate(0deg);
  transition: transform 0.5s ease;
}

.theme-toggle.switching {
  transform: rotate(360deg);
}
```

---

## 🧪 测试计划

### 单元测试（可选）
```javascript
// tests/theme.spec.js
describe('Theme System', () => {
  it('should switch theme on click', () => {
    // 测试主题切换功能
  });

  it('should persist theme in localStorage', () => {
    // 测试持久化
  });
});
```

### 手动测试清单

#### 功能测试
- [ ] 点击主题切换按钮，主题立即切换
- [ ] 深色模式下所有文本清晰可读
- [ ] 深色模式下所有图标清晰可见
- [ ] 深色模式下表格数据易于区分
- [ ] 深色模式下按钮状态清晰（正常、悬停、禁用）
- [ ] 主题选择刷新页面后保持

#### 视觉测试
- [ ] 所有页面在深色模式下美观
- [ ] Glass Morphism效果在深色模式下正常
- [ ] 渐变色在深色模式下协调
- [ ] 阴影效果在深色模式下明显

#### 性能测试
- [ ] 主题切换无卡顿（<100ms）
- [ ] Lighthouse性能评分不下降
- [ ] 内存占用无明显增加

#### 兼容性测试
- [ ] Chrome最新版 ✅
- [ ] Firefox最新版 ✅
- [ ] Safari最新版 ✅
- [ ] Edge最新版 ✅
- [ ] 移动端Chrome ✅
- [ ] 移动端Safari ✅

---

## 📦 技术实现

### 主题配置（theme.js）
```javascript
export const themes = {
  light: {
    name: '亮色模式',
    key: 'light',
    icon: '☀️'
  },
  dark: {
    name: '深色模式',
    key: 'dark',
    icon: '🌙'
  }
};

export function getCurrentTheme() {
  return localStorage.getItem('theme') || 'light';
}

export function setTheme(theme) {
  localStorage.setItem('theme', theme);
  document.documentElement.setAttribute('data-theme', theme);

  // 触发事件通知其他组件
  window.dispatchEvent(new CustomEvent('theme-changed', {
    detail: { theme }
  }));
}

export function initTheme() {
  const theme = getCurrentTheme();
  setTheme(theme);
}
```

### 深色主题CSS（theme-dark.css）
```css
[data-theme="dark"] {
  /* 背景色 */
  --bg-primary: #0a0e27;
  --bg-secondary: #0f0f1e;
  --bg-tertiary: #16213e;
  --bg-elevated: #1a1a2e;

  /* 文本色 */
  --text-primary: #ffffff;
  --text-secondary: #e0e0e0;
  --text-tertiary: #b0b0b0;

  /* 边框色 */
  --border-color: rgba(255, 255, 255, 0.1);

  /* Glass Morphism */
  --glass-bg: rgba(255, 255, 255, 0.05);
  --glass-border: rgba(255, 255, 255, 0.1);
  --glass-shadow: 0 8px 32px rgba(0, 0, 0, 0.5);

  /* 输入框 */
  --input-bg: rgba(255, 255, 255, 0.05);
  --input-border: rgba(255, 255, 255, 0.1);
  --input-text: #ffffff;

  /* 按钮 */
  --button-bg: rgba(255, 255, 255, 0.1);
  --button-hover: rgba(255, 255, 255, 0.15);
  --button-active: rgba(255, 255, 255, 0.2);
}
```

### 主题切换组件（ThemeToggle.vue）
```vue
<script setup>
import { ref, onMounted } from 'vue';
import { getCurrentTheme, setTheme, themes } from '@/config/theme';

const currentTheme = ref('light');
const isSwitching = ref(false);

const toggleTheme = () => {
  isSwitching.value = true;

  const newTheme = currentTheme.value === 'light' ? 'dark' : 'light';
  currentTheme.value = newTheme;
  setTheme(newTheme);

  setTimeout(() => {
    isSwitching.value = false;
  }, 500);
};

onMounted(() => {
  currentTheme.value = getCurrentTheme();
});
</script>

<template>
  <button
    class="theme-toggle"
    :class="{ switching: isSwitching }"
    @click="toggleTheme"
    :title="currentTheme === 'light' ? '切换到深色模式' : '切换到亮色模式'"
  >
    <span class="theme-icon">
      {{ currentTheme === 'light' ? '🌙' : '☀️' }}
    </span>
  </button>
</template>

<style scoped>
.theme-toggle {
  width: 44px;
  height: 44px;
  border-radius: 50%;
  background: var(--glass-bg);
  backdrop-filter: blur(10px);
  border: 1px solid var(--glass-border);
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: all 0.3s ease;
}

.theme-toggle:hover {
  background: var(--glass-hover, rgba(255, 255, 255, 0.15));
  transform: scale(1.05);
}

.theme-toggle.switching .theme-icon {
  animation: rotate 0.5s ease;
}

@keyframes rotate {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}

.theme-icon {
  font-size: 20px;
  display: block;
}
</style>
```

---

## 🚀 部署计划

### 开发环境验证
```bash
# 1. 开发模式测试
cd src/web/vue-app
npm run dev

# 2. 验证主题切换功能
# 在浏览器中测试所有页面

# 3. 验证主题持久化
# 刷新页面，主题应保持
```

### 生产构建
```bash
# 构建生产版本
npm run build

# 验证构建产物
ls -la ../dist/assets/css/
# 应该看到新的CSS文件包含深色主题

# 部署验证
./verify-deployment.sh
```

### 灰度发布（可选）
```javascript
// 默认深色模式开启，可通过配置控制
const ENABLE_DARK_MODE = true;

if (ENABLE_DARK_MODE) {
  // 显示主题切换按钮
}
```

---

## 📊 成功指标

### 功能指标
- [x] 主题切换功能100%可用
- [x] 所有10个页面支持深色模式
- [x] 主题持久化成功率100%
- [x] 主题切换响应时间<100ms

### 质量指标
- [x] Lighthouse Accessibility ≥90分
- [x] Lighthouse Performance ≥95分
- [x] 所有文本对比度符合WCAG AA标准
- [x] 跨浏览器兼容性100%

### 用户体验指标
- [ ] 用户反馈正面率>80%
- [ ] 深色模式使用率>30%（夜间）
- [ ] 无深色模式相关Bug报告

---

## 🐛 风险预案

### 风险1: 颜色对比度不足
**预防：** 使用对比度检查工具
**应对：** 调整颜色亮度，确保可读性

### 风险2: 主题切换卡顿
**预防：** 使用CSS变量减少重绘
**应对：** 优化过渡动画，使用GPU加速

### 风险3: 某些组件深色模式异常
**预防：** 逐个组件测试
**应对：** 单独为异常组件添加深色样式

---

## 📝 验收标准

### 必须达成（P0）
- [x] 主题切换按钮可见且可用
- [x] 点击按钮主题立即切换
- [x] 刷新页面主题保持不变
- [x] 所有10个页面在深色模式下可用
- [x] 所有文本在深色模式下清晰可读
- [x] Lighthouse Accessibility ≥90分

### 应该达成（P1）
- [ ] 主题切换动画流畅
- [ ] 所有页面在深色模式下美观
- [ ] 移动端深色模式体验良好
- [ ] 深色模式使用率>20%

### 可以达成（P2）
- [ ] 支持自动切换（根据系统设置）
- [ ] 支持定时切换（白天/夜间）
- [ ] 添加主题预览功能

---

**计划创建时间：** 2026-01-03
**预计完成时间：** 2026-01-06（3-4天）
**负责人：** 开发团队
**状态：** 🚀 准备开始
