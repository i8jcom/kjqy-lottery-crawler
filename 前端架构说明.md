# 彩票爬虫系统 - 前端架构说明

## 📋 目录

- [技术栈](#技术栈)
- [项目结构](#项目结构)
- [页面组件](#页面组件)
- [通用组件](#通用组件)
- [路由配置](#路由配置)
- [主题系统](#主题系统)
- [状态管理](#状态管理)
- [实时通信](#实时通信)
- [HTTP API封装](#http-api封装)
- [构建部署](#构建部署)
- [开发规范](#开发规范)

---

## 技术栈

### 核心框架

```json
{
  "framework": "Vue 3.3+",
  "composition-api": "setup + <script setup>",
  "ui-library": "Element Plus 2.4+",
  "build-tool": "Vite 5.0+",
  "router": "Vue Router 4.2+",
  "http-client": "axios 1.6+",
  "websocket": "原生WebSocket API",
  "package-manager": "npm"
}
```

### 开发工具

- **代码规范**: ESLint + Prettier
- **样式预处理**: CSS（Element Plus自带主题）
- **开发服务器**: Vite Dev Server (HMR热更新)
- **路由进度条**: NProgress
- **浏览器支持**: Chrome 90+, Firefox 88+, Safari 14+

### UI特性

- **主题系统**: 深色主题（默认）+ 亮色主题
- **响应式设计**: 适配桌面和平板设备
- **组件库**: Element Plus 完整组件集
- **图表库**: ECharts (通过 vue-echarts 集成)

---

## 项目结构

```
src/web/vue-app/
├── index.html                # HTML模板入口
├── src/
│   ├── main.js               # 应用入口
│   ├── App.vue               # 根组件
│   │
│   ├── router/               # 路由配置
│   │   └── index.js          # 15个路由 + NProgress配置
│   │
│   ├── services/             # 业务服务
│   │   ├── api.js            # HTTP API封装 (axios)
│   │   └── websocket.js      # WebSocket服务
│   │
│   ├── views/                # 页面组件 (15个，100% Element Plus)
│   │   ├── DashboardElementPlus.vue          # 系统仪表盘
│   │   ├── SystemMonitorElementPlus.vue      # 系统监控
│   │   ├── RealtimeElementPlus.vue           # 实时彩种
│   │   ├── SchedulerElementPlus.vue          # 调度管理
│   │   ├── HistoryElementPlus.vue            # 历史查询
│   │   ├── DataManagementElementPlus.vue     # 数据管理
│   │   ├── AlertsLuxuryElementPlus.vue       # 告警管理
│   │   ├── SettingsElementPlus.vue           # 系统设置
│   │   ├── SourcesElementPlus.vue            # 数据源管理
│   │   ├── LotteryConfigsElementPlus.vue     # 彩种配置
│   │   ├── LogsProElementPlus.vue            # 日志管理
│   │   ├── DomainManagementElementPlus.vue   # 域名管理
│   │   ├── WebSocketMonitorElementPlus.vue   # WebSocket监控
│   │   ├── DataCompletionElementPlus.vue     # 数据补全
│   │   ├── ElementThemeTest.vue              # 主题测试
│   │
│   ├── components/           # 通用组件 (25个，按功能分类)
│   │   ├── charts/                   # 图表组件 (3个)
│   │   │   ├── LineChart.vue
│   │   │   ├── BarChart.vue
│   │   │   └── PieChart.vue
│   │   ├── common/                   # 通用组件 (7个)
│   │   │   ├── CountdownTimer.vue
│   │   │   ├── LotteryBall.vue
│   │   │   ├── LoadingSpinner.vue
│   │   │   ├── EmptyState.vue
│   │   │   ├── PageHeader.vue
│   │   │   ├── SearchBar.vue
│   │   │   └── StatusBadge.vue
│   │   ├── layout/                   # 布局组件 (3个)
│   │   │   ├── MainLayout.vue
│   │   │   ├── Sidebar.vue
│   │   │   └── TopBar.vue
│   │   ├── modals/                   # 弹窗组件 (2个)
│   │   │   ├── ConfirmDialog.vue
│   │   │   └── DetailModal.vue
│   │   ├── tech/                     # 技术组件 (5个)
│   │   │   ├── WebSocketStatus.vue
│   │   │   ├── ApiHealth.vue
│   │   │   ├── PerformanceMonitor.vue
│   │   │   ├── ErrorBoundary.vue
│   │   │   └── DebugPanel.vue
│   │   ├── widgets/                  # 小部件 (3个)
│   │   │   ├── StatCard.vue
│   │   │   ├── RecentActivity.vue
│   │   │   └── QuickActions.vue
│   │   ├── feedback/                 # 反馈组件 (1个)
│   │   │   └── ToastNotification.vue
│   │   └── HelloWorld.vue            # 示例组件
│   │
│   └── assets/               # 资源文件
│       ├── styles/           # 全局样式
│       └── images/           # 图片资源
│
├── vite.config.js            # Vite配置
├── package.json              # 项目依赖
└── .eslintrc.js              # ESLint配置

# 构建产物输出到上级目录
src/web/dist/                 # ✅ 构建产物目录
├── index.html                # 入口HTML (4.6 KB)
├── vite.svg                  # 图标
└── assets/
    ├── css/                  # 23个CSS文件 (566 KB)
    │   ├── vendor-libs.css   # Element Plus等 (347 KB)
    │   ├── index.css         # 主应用样式 (89 KB)
    │   └── [页面组件CSS...]
    ├── js/                   # 47个JS文件 (2.7 MB)
    │   ├── vendor-libs.js    # 第三方库 (1.35 MB)
    │   ├── charts.js         # ECharts (892 KB)
    │   ├── vue-vendor.js     # Vue核心 (42 KB)
    │   └── [页面组件JS...]
    └── lottery-balls/        # 彩球图片资源
```

---

## 页面组件

### Element Plus 版本页面 (15个)

系统核心功能页面，使用 Element Plus UI 框架：

1. **DashboardElementPlus.vue** - 系统仪表盘
   - 彩种总览卡片
   - 系统运行统计
   - 实时爬取状态
   - 告警概览

2. **SystemMonitorElementPlus.vue** - 系统监控
   - 系统资源使用（CPU、内存）
   - 调度器状态
   - WebSocket连接数
   - 性能指标图表

3. **RealtimeElementPlus.vue** - 实时彩种数据
   - 高频彩/低频彩分类显示
   - 实时倒计时
   - 最新开奖结果
   - WebSocket实时推送

4. **SchedulerElementPlus.vue** - 调度管理
   - 调度任务列表
   - 启动/停止/重启调度器
   - 调度统计信息
   - 任务执行历史

5. **HistoryElementPlus.vue** - 历史数据查询
   - 彩种选择器
   - 日期范围选择
   - 分页查询
   - CSV导出功能
   - 金多宝标记显示

6. **DataManagementElementPlus.vue** - 数据管理工具
   - 手动触发爬取
   - 数据回填
   - 缺失数据检测
   - 数据清理

7. **AlertsLuxuryElementPlus.vue** - 告警管理
   - 告警历史记录
   - 告警级别筛选
   - 告警规则配置
   - 告警统计图表

8. **SettingsElementPlus.vue** - 系统设置
   - 系统参数配置
   - 告警配置（钉钉、企业微信）
   - 主题切换
   - 功能开关

9. **SourcesElementPlus.vue** - 数据源管理
   - 数据源列表
   - 启用/禁用数据源
   - 数据源测试
   - 优先级配置

10. **LotteryConfigsElementPlus.vue** - 彩种配置
    - 彩种列表管理
    - 启用/禁用彩种
    - 调度间隔配置
    - 彩种参数设置

11. **LogsProElementPlus.vue** - 日志管理
    - 爬虫日志查看
    - 日志级别筛选
    - 日志搜索
    - 日志导出

12. **DomainManagementElementPlus.vue** - 域名管理
    - 域名池列表
    - 域名健康状态
    - 域名切换历史
    - 手动切换域名

13. **WebSocketMonitorElementPlus.vue** - WebSocket监控
    - WebSocket连接列表
    - 连接状态监控
    - 消息统计
    - 连接延迟分析

14. **DataCompletionElementPlus.vue** - 数据补全工具
    - 缺失数据检测
    - 自动补全功能
    - 补全历史记录
    - 补全进度跟踪

15. **ElementThemeTest.vue** - 主题测试页面
    - 主题切换测试
    - Element Plus组件展示
    - 颜色变量预览

**UI一致性说明**:
- ✅ 100%使用Element Plus组件库
- ✅ 统一的深色主题设计
- ✅ 统一的用户交互体验
- ✅ 所有页面支持路由懒加载
- ✅ 无旧版UI混用，确保体验一致

---

## 通用组件

### 1. 图表组件 (charts/) - 3个

**LineChart.vue** - 折线图组件
- 基于ECharts封装
- 支持多条数据线
- 响应式尺寸
- 自定义颜色主题

**BarChart.vue** - 柱状图组件
- 横向/纵向切换
- 数据标签显示
- 动画效果

**PieChart.vue** - 饼图组件
- 圆环/实心切换
- 数据百分比显示
- 图例配置

---

### 2. 通用组件 (common/) - 7个

**CountdownTimer.vue** - 倒计时显示
- 时分秒格式化
- 实时倒计时
- 样式可配置

**LotteryBall.vue** - 彩球号码显示
- 彩球样式渲染
- 号码颜色分类
- 大小可配置

**LoadingSpinner.vue** - 加载动画
- 多种动画效果
- 大小和颜色可配置

**EmptyState.vue** - 空状态提示
- 自定义图标
- 自定义文案
- 操作按钮支持

**PageHeader.vue** - 页面头部
- 标题和描述
- 面包屑导航
- 操作按钮区域

**SearchBar.vue** - 搜索栏
- 关键词搜索
- 搜索建议
- 高级筛选

**StatusBadge.vue** - 状态徽章
- 状态颜色映射
- 动态/静态模式
- 图标支持

---

### 3. 布局组件 (layout/) - 3个

**MainLayout.vue** - 主布局容器
- 侧边栏+主内容区
- 响应式折叠
- 深色/亮色主题切换

**Sidebar.vue** - 侧边栏导航
- 菜单项配置
- 图标+文字
- 折叠/展开动画
- 路由高亮

**TopBar.vue** - 顶部导航栏
- 系统标题
- 快捷操作
- 用户信息
- 主题切换按钮

---

### 4. 弹窗组件 (modals/) - 2个

**ConfirmDialog.vue** - 确认对话框
- 确认/取消操作
- 自定义标题和内容
- 危险操作警告

**DetailModal.vue** - 详情弹窗
- 大尺寸弹窗
- 数据详情展示
- 可滚动内容区

---

### 5. 技术组件 (tech/) - 5个

**WebSocketStatus.vue** - WebSocket连接状态
- 连接指示灯
- 连接/断开时间
- 消息统计

**ApiHealth.vue** - API健康检查
- API响应时间
- 成功率统计
- 错误日志

**PerformanceMonitor.vue** - 性能监控
- FPS显示
- 内存使用
- 网络请求统计

**ErrorBoundary.vue** - 错误边界
- 捕获组件错误
- 错误信息展示
- 错误上报

**DebugPanel.vue** - 调试面板
- 开发环境调试工具
- 状态查看
- API测试

---

### 6. 小部件 (widgets/) - 3个

**StatCard.vue** - 统计卡片
- 数值显示
- 趋势指示
- 图标装饰

**RecentActivity.vue** - 最近活动
- 活动时间线
- 活动类型图标
- 时间格式化

**QuickActions.vue** - 快速操作
- 常用操作按钮
- 图标+文字
- 快捷键提示

---

### 7. 反馈组件 (feedback/) - 1个

**ToastNotification.vue** - 消息通知
- 成功/警告/错误类型
- 自动关闭
- 位置可配置

---

## 路由配置

### 路由总览

**总路由数**: 15个（包含1个重定向）

**路由文件**: `src/router/index.js`

### NProgress 配置

路由切换时显示顶部进度条：

```javascript
import NProgress from 'nprogress'
import 'nprogress/nprogress.css'

NProgress.configure({
  easing: 'ease',
  speed: 500,
  showSpinner: false,
  trickleSpeed: 200,
  minimum: 0.3
})
```

### 路由列表

```javascript
const routes = [
  {
    path: '/',
    redirect: '/dashboard'
  },
  {
    path: '/dashboard',
    name: 'dashboard',
    component: () => import('../views/DashboardElementPlus.vue'),
    meta: { title: '系统仪表盘' }
  },
  {
    path: '/system-monitor',
    name: 'system-monitor',
    component: () => import('../views/SystemMonitorElementPlus.vue'),
    meta: { title: '系统监控' }
  },
  {
    path: '/realtime',
    name: 'realtime',
    component: () => import('../views/RealtimeElementPlus.vue'),
    meta: { title: '实时彩种' }
  },
  {
    path: '/scheduler',
    name: 'scheduler',
    component: () => import('../views/SchedulerElementPlus.vue'),
    meta: { title: '调度管理' }
  },
  {
    path: '/history',
    name: 'history',
    component: () => import('../views/HistoryElementPlus.vue'),
    meta: { title: '历史查询' }
  },
  {
    path: '/data-management',
    name: 'data-management',
    component: () => import('../views/DataManagementElementPlus.vue'),
    meta: { title: '数据管理' }
  },
  {
    path: '/alerts',
    name: 'alerts',
    component: () => import('../views/AlertsLuxuryElementPlus.vue'),
    meta: { title: '告警管理' }
  },
  {
    path: '/settings',
    name: 'settings',
    component: () => import('../views/SettingsElementPlus.vue'),
    meta: { title: '系统设置' }
  },
  {
    path: '/sources',
    name: 'sources',
    component: () => import('../views/SourcesElementPlus.vue'),
    meta: { title: '数据源管理' }
  },
  {
    path: '/lottery-configs',
    name: 'lottery-configs',
    component: () => import('../views/LotteryConfigsElementPlus.vue'),
    meta: { title: '彩种配置' }
  },
  {
    path: '/logs',
    name: 'logs',
    component: () => import('../views/LogsProElementPlus.vue'),
    meta: { title: '日志管理' }
  },
  {
    path: '/domain-management',
    name: 'domain-management',
    component: () => import('../views/DomainManagementElementPlus.vue'),
    meta: { title: '域名管理' }
  },
  {
    path: '/websocket-monitor',
    name: 'websocket-monitor',
    component: () => import('../views/WebSocketMonitorElementPlus.vue'),
    meta: { title: 'WebSocket监控' }
  },
  {
    path: '/data-completion',
    name: 'data-completion',
    component: () => import('../views/DataCompletionElementPlus.vue'),
    meta: { title: '数据补全' }
  }
]
```

### 路由增强功能

1. **懒加载**: 所有页面组件使用动态导入（`import()`），减少初始加载体积
2. **进度条**: 路由切换时自动显示NProgress进度条
3. **元信息**: 每个路由包含`meta.title`用于设置页面标题
4. **路由守卫**: 可以添加权限验证（当前未启用）

---

## 主题系统

### 主题支持

- **深色主题**（默认）: 适合长时间监控使用
- **亮色主题**: 适合明亮环境使用

### 实现方式

在 `index.html` 中通过CSS变量定义主题：

```html
<style>
  /* 深色主题（默认） */
  :root {
    --el-text-color-primary: #ffffff !important;
    --el-text-color-regular: #e0e0e0 !important;
    --el-text-color-secondary: #b0b0b0 !important;
    --el-text-color-placeholder: #808080 !important;
    --el-bg-color: #1a1a2e !important;
    --el-bg-color-page: #0a0e27 !important;
    --el-bg-color-overlay: #1a1a2e !important;
    --el-border-color: rgba(255, 255, 255, 0.1) !important;
    --el-border-color-light: rgba(255, 255, 255, 0.08) !important;
    --el-fill-color: rgba(255, 255, 255, 0.05) !important;
    --el-fill-color-light: rgba(255, 255, 255, 0.03) !important;
    --el-fill-color-blank: #1a1a2e !important;
  }

  /* 亮色主题 */
  html[data-theme="light"] {
    --el-text-color-primary: #3d4451 !important;
    --el-text-color-regular: #5a6270 !important;
    --el-text-color-secondary: #707886 !important;
    --el-text-color-placeholder: #8b92a0 !important;
    --el-bg-color: #d5d7e0 !important;
    --el-bg-color-page: #c8cad3 !important;
    --el-bg-color-overlay: #d5d7e0 !important;
    --el-border-color: rgba(0, 0, 0, 0.1) !important;
    --el-border-color-light: rgba(0, 0, 0, 0.08) !important;
    --el-fill-color: #cecfd9 !important;
    --el-fill-color-light: #d5d7e0 !important;
    --el-fill-color-blank: #d5d7e0 !important;
  }
</style>
```

### 主题切换

```javascript
// 切换到亮色主题
document.documentElement.setAttribute('data-theme', 'light');

// 切换到深色主题
document.documentElement.removeAttribute('data-theme');

// 持久化主题设置
localStorage.setItem('theme', 'light');
```

### 加载优化

**加载动画**:
```html
<div id="app-loading">
  <div class="loading-spinner"></div>
  <div class="loading-text">正在加载爬虫管理系统...</div>
</div>
```

**超时检测**:
```javascript
// 5秒后如果Vue还未挂载，显示错误提示
setTimeout(() => {
  if (document.getElementById('app-loading')) {
    document.getElementById('error-msg').style.display = 'block';
  }
}, 5000);
```

**缓存控制**:
```html
<!-- 禁止HTML文件缓存，确保始终加载最新版本 -->
<meta http-equiv="Cache-Control" content="no-cache, no-store, must-revalidate" />
<meta http-equiv="Pragma" content="no-cache" />
<meta http-equiv="Expires" content="0" />
```

---

## 状态管理

### 当前实现

系统采用**组件内部状态管理**，不使用 Vuex/Pinia 等全局状态管理库。

### 状态管理方式

1. **组件内状态**: 使用 Vue 3 Composition API（`ref` / `reactive`）
   ```javascript
   const lotteries = ref([]);
   const loading = ref(false);
   const pagination = reactive({ page: 1, limit: 50, total: 0 });
   ```

2. **跨组件通信**:
   - **Props / Emit**: 父子组件通信
   - **Provide / Inject**: 跨层级组件通信
   - **Event Bus**: 兄弟组件通信（少用）

3. **全局共享数据**:
   - **WebSocket连接状态**: 在 `websocket.js` 中维护
   - **彩种列表**: 通过API获取后缓存在组件内
   - **用户设置**: 存储在 `localStorage`

### 未来考虑

如果状态管理复杂度增加，可以引入 **Pinia** 进行全局状态管理。

---

## 实时通信

### WebSocket服务封装

**文件**: `src/services/websocket.js`

```javascript
let ws = null;
let messageHandler = null;
let reconnectTimer = null;

// 建立连接
export function connectWebSocket(onMessage) {
  messageHandler = onMessage;

  const protocol = window.location.protocol === 'https:' ? 'wss:' : 'ws:';
  const host = window.location.host;
  const wsUrl = `${protocol}//${host}`;

  ws = new WebSocket(wsUrl);

  ws.onopen = () => {
    console.log('[WebSocket] 连接成功');
    wsConnected.value = true;

    // 清除重连定时器
    if (reconnectTimer) {
      clearTimeout(reconnectTimer);
      reconnectTimer = null;
    }
  };

  ws.onmessage = (event) => {
    try {
      const message = JSON.parse(event.data);
      messageHandler(message);
    } catch (error) {
      console.error('[WebSocket] 消息解析失败:', error);
    }
  };

  ws.onerror = (error) => {
    console.error('[WebSocket] 连接错误:', error);
    wsConnected.value = false;
  };

  ws.onclose = () => {
    console.log('[WebSocket] 连接关闭');
    wsConnected.value = false;

    // 5秒后自动重连
    reconnectTimer = setTimeout(() => {
      console.log('[WebSocket] 尝试重连...');
      connectWebSocket(messageHandler);
    }, 5000);
  };
}

// 断开连接
export function disconnectWebSocket() {
  if (ws) {
    ws.close();
    ws = null;
  }

  if (reconnectTimer) {
    clearTimeout(reconnectTimer);
    reconnectTimer = null;
  }
}

// 发送消息
export function sendMessage(message) {
  if (ws && ws.readyState === WebSocket.OPEN) {
    ws.send(JSON.stringify(message));
  } else {
    console.warn('[WebSocket] 未连接，无法发送消息');
  }
}
```

### WebSocket消息格式

**服务器推送**:
```javascript
{
  type: 'lottery_update',    // 消息类型
  lotCode: '10001',          // 彩种代码
  data: {
    issue: '2026011701',     // 期号
    drawCode: '01,02,03',    // 开奖号码
    drawTime: '2026-01-17 21:30:00',
    countdown: 278           // 倒计时（秒）
  }
}
```

**其他消息类型**:
- `alert_new`: 新告警通知
- `domain_switch`: 域名切换通知
- `system_status`: 系统状态更新

### 使用示例

```vue
<script setup>
import { ref, onMounted, onUnmounted } from 'vue';
import { connectWebSocket, disconnectWebSocket } from '@/services/websocket';

const wsConnected = ref(false);

onMounted(() => {
  connectWebSocket((message) => {
    if (message.type === 'lottery_update') {
      // 处理开奖更新
      console.log('新开奖:', message.data);
    }
  });
});

onUnmounted(() => {
  disconnectWebSocket();
});
</script>
```

---

## HTTP API封装

**文件**: `src/services/api.js`

```javascript
import axios from 'axios';
import { ElMessage } from 'element-plus';

// 创建axios实例
const api = axios.create({
  baseURL: '/api',
  timeout: 60000,  // 60秒超时（历史数据查询可能较慢）
  headers: {
    'Content-Type': 'application/json'
  }
});

// 请求拦截器
api.interceptors.request.use(
  (config) => {
    // 可以在此添加token等认证信息
    return config;
  },
  (error) => {
    return Promise.reject(error);
  }
);

// 响应拦截器
api.interceptors.response.use(
  (response) => {
    return response.data;
  },
  (error) => {
    // 统一错误处理
    if (error.code === 'ECONNABORTED') {
      ElMessage.error('请求超时，请稍后重试');
    } else if (error.response) {
      const status = error.response.status;
      if (status === 404) {
        ElMessage.error('请求的资源不存在');
      } else if (status === 500) {
        ElMessage.error('服务器错误，请联系管理员');
      } else {
        ElMessage.error(error.response.data.message || '请求失败');
      }
    } else {
      ElMessage.error('网络错误，请检查网络连接');
    }

    return Promise.reject(error);
  }
);

// API接口
export const getLotteries = () => api.get('/lotteries');
export const getLatestResult = (lotCode) => api.get(`/lottery/${lotCode}/latest`);
export const getHistory = (lotCode, date, page, limit) =>
  api.get(`/lottery/${lotCode}/history`, { params: { date, page, limit } });
export const getSystemStatus = () => api.get('/system/status');
export const getAlertHistory = (params) => api.get('/alerts/history', { params });
export const getDomainHealth = () => api.get('/domains/health');

export default api;
```

---

## 构建部署

### Vite配置

**文件**: `vite.config.js`

```javascript
import { defineConfig } from 'vite';
import vue from '@vitejs/plugin-vue';
import path from 'path';

export default defineConfig({
  plugins: [vue()],

  // 路径别名
  resolve: {
    alias: {
      '@': path.resolve(__dirname, 'src')
    }
  },

  // 构建配置
  build: {
    outDir: '../dist',           // 输出到上级dist目录
    assetsDir: 'assets',         // 资源文件目录
    sourcemap: false,            // 生产环境不生成sourcemap
    minify: 'terser',            // 使用terser压缩
    chunkSizeWarningLimit: 1000, // chunk大小警告阈值

    rollupOptions: {
      output: {
        // 代码分割
        manualChunks: {
          'element-plus': ['element-plus'],
          'vue': ['vue', 'vue-router'],
          'axios': ['axios']
        }
      }
    }
  },

  // 开发服务器
  server: {
    port: 4001,              // 开发服务器端口
    proxy: {
      '/api': {
        target: 'http://localhost:4000',  // 代理到后端
        changeOrigin: true
      },
      '/ws': {
        target: 'ws://localhost:4000',
        ws: true
      }
    }
  }
});
```

### 构建命令

```bash
# 开发模式（热更新）
npm run dev

# 生产构建
npm run build

# 预览构建产物
npm run preview
```

### 构建流程

```
源代码
  ↓
Vite编译
  ↓
代码分割
  ├─ vendor-libs.js      (1.35 MB - Element Plus, axios等)
  ├─ charts.js           (892 KB - ECharts)
  ├─ vue-vendor.js       (42 KB - Vue核心)
  └─ [页面组件...]       (各10-30 KB)
  ↓
压缩优化 (terser)
  ↓
Gzip压缩 (67%压缩率)
  ↓
输出到 src/web/dist/
  ├── index.html (4.6 KB)
  ├── assets/css/ (566 KB → 79 KB gzip)
  └── assets/js/ (2.7 MB → 918 KB gzip)
  ↓
WebServer.js 静态文件服务
```

### 部署注意事项

1. **每次修改前端代码后必须重新构建**
   ```bash
   cd src/web/vue-app
   npm run build
   ```

2. **构建产物位置**: `src/web/dist/`（不是 `vue-app/dist/`）

3. **WebServer使用的是构建产物**，不是源码

4. **Docker部署**:
   ```dockerfile
   # Dockerfile
   FROM node:18-alpine
   WORKDIR /app

   # 安装依赖
   COPY package*.json ./
   RUN npm install --production

   # 复制源码
   COPY . .

   # 构建前端
   WORKDIR /app/src/web/vue-app
   RUN npm install && npm run build

   # 返回根目录
   WORKDIR /app

   # 启动服务
   CMD ["node", "src/index.js"]
   ```

5. **快速重建脚本**:
   ```bash
   bash scripts/rebuild-and-restart.sh
   ```

---

## 开发规范

### 1. 组件命名

- **页面组件**: `PascalCase` + `ElementPlus` 后缀
  - 例如: `RealtimeElementPlus.vue`
- **通用组件**: `PascalCase`
  - 例如: `CountdownTimer.vue`

### 2. 代码风格

**Vue 3 Composition API + `<script setup>`**:

```vue
<script setup>
import { ref, computed, onMounted } from 'vue';

// Props定义
const props = defineProps({
  lottery: Object,
  countdown: Number
});

// Emits定义
const emit = defineEmits(['refresh', 'update']);

// 响应式数据
const loading = ref(false);

// 计算属性
const formattedCountdown = computed(() => {
  return formatTime(props.countdown);
});

// 方法
function handleRefresh() {
  emit('refresh');
}

// 生命周期
onMounted(() => {
  console.log('组件已挂载');
});
</script>

<template>
  <div class="component">
    <!-- 模板内容 -->
  </div>
</template>

<style scoped>
.component {
  /* 样式 */
}
</style>
```

### 3. 异步处理

**使用 async/await**:

```javascript
async function fetchData() {
  loading.value = true;
  try {
    const response = await api.get('/data');
    data.value = response.data;
  } catch (error) {
    ElMessage.error('加载失败: ' + error.message);
  } finally {
    loading.value = false;
  }
}
```

### 4. 错误处理

**统一错误提示**:

```javascript
import { ElMessage, ElMessageBox } from 'element-plus';

// 成功提示
ElMessage.success('操作成功');

// 警告提示
ElMessage.warning('请先选择彩种');

// 错误提示
ElMessage.error('网络错误，请重试');

// 确认对话框
ElMessageBox.confirm('确定要删除吗？', '提示', {
  confirmButtonText: '确定',
  cancelButtonText: '取消',
  type: 'warning'
}).then(() => {
  // 确认操作
}).catch(() => {
  // 取消操作
});
```

### 5. 性能优化

**避免不必要的渲染**:

```vue
<script setup>
import { ref, computed, watch } from 'vue';

// 使用computed缓存计算结果
const filteredData = computed(() => {
  return data.value.filter(item => item.status === 'active');
});

// 使用watch监听特定变化
watch(() => props.lottery, (newVal) => {
  fetchLatestResult(newVal.lotCode);
}, { immediate: true });
</script>
```

**列表渲染优化**:

```vue
<template>
  <!-- 使用key优化列表渲染 -->
  <div v-for="lottery in lotteries" :key="lottery.lotCode">
    {{ lottery.lotName }}
  </div>

  <!-- 虚拟滚动（大数据量） -->
  <el-table
    :data="tableData"
    :virtual="true"
    :height="600"
  />
</template>
```

---

## 常见问题

### Q1: 前端修改后不生效？

**原因**: 未重新构建或浏览器缓存

**解决**:
```bash
# 1. 重新构建
cd src/web/vue-app
npm run build

# 2. 重启服务
cd ../../..
docker-compose restart lottery-crawler

# 3. 清除浏览器缓存
Ctrl + Shift + R（硬刷新）
```

### Q2: WebSocket连接失败？

**排查**:
1. 检查后端WebSocket服务是否启动
2. 检查防火墙是否阻止WebSocket连接
3. 检查浏览器控制台错误信息

### Q3: 倒计时不准确？

**原因**: WebSocket推送延迟或本地计算误差

**解决**: 使用混合策略（WebSocket推送 + 本地计算 + 定期校准）

### Q4: Element Plus样式异常？

**原因**: CSS加载顺序或覆盖问题

**解决**:
```javascript
// main.js 中正确导入Element Plus样式
import 'element-plus/dist/index.css';
```

### Q5: Chrome DevTools CSP警告？

**现象**: 控制台显示 `Content-Security-Policy: default-src 'none'` 警告

**影响**: 仅影响Chrome DevTools调试功能，不影响应用正常运行

**解决**: 如需完全消除，可以调整 WebServer.js 的CSP策略

---

## 相关文档

- [部署指南.md](./部署指南.md) - 部署和运维
- [项目架构说明.md](./项目架构说明.md) - 后端架构
- [前端构建与部署指南.md](./前端构建与部署指南.md) - 前端开发流程
- [docs/API_REFERENCE.md](./docs/API_REFERENCE.md) - 完整API接口文档
- [Element Plus官方文档](https://element-plus.org/)
- [Vue 3官方文档](https://cn.vuejs.org/)
- [Vite官方文档](https://cn.vitejs.dev/)
