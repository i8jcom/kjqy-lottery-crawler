# 彩票爬虫系统部署指南

## 📋 目录

- [系统要求](#系统要求)
- [快速开始](#快速开始)
- [Docker部署](#docker部署)
- [手动部署](#手动部署)
- [配置说明](#配置说明)
- [常见问题](#常见问题)
- [维护与监控](#维护与监控)

---

## 系统要求

### 硬件要求
- CPU: 2核心+
- 内存: 4GB+
- 磁盘: 20GB+（数据库会持续增长）

### 软件要求
- **Docker + Docker Compose** (推荐方式)
  - Docker 20.10+
  - Docker Compose 2.0+

或

- **手动部署**
  - Node.js 18+
  - MySQL 8.0+
  - npm 或 yarn

---

## 快速开始

### 使用 Docker Compose（推荐）

```bash
# 1. 克隆项目
git clone https://github.com/i8jcom/kjqy-lottery-crawler.git
cd kjqy-lottery-crawler

# 2. 启动所有服务
docker-compose up -d

# 3. 查看日志
docker-compose logs -f crawler-service

# 4. 访问管理界面
http://localhost:4000
```

**启动后的服务**：
- 爬虫服务 + Web管理界面：http://localhost:4000
- MySQL数据库：宿主机访问 localhost:3308，容器间通过 mysql:3306

---

## Docker部署

### 1. 项目结构

```
crawler-service/
├── docker-compose.yml       # Docker Compose配置
├── Dockerfile              # 爬虫服务镜像
├── init.sql                # 数据库初始化脚本（10个表）
├── my.cnf                  # MySQL配置
└── .env                    # 环境变量（可选）
```

### 2. 配置说明

#### docker-compose.yml

```yaml
services:
  # MySQL数据库
  mysql:
    image: mysql:8.0
    container_name: lottery-mysql-compose
    restart: unless-stopped
    ports:
      - "3308:3306"
    environment:
      - MYSQL_ROOT_PASSWORD=root123456
      - MYSQL_DATABASE=lottery_crawler
      - MYSQL_USER=lottery
      - MYSQL_PASSWORD=lottery123
      - TZ=Asia/Shanghai
    volumes:
      - mysql-data:/var/lib/mysql
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql:ro
      - ./my.cnf:/etc/mysql/conf.d/my.cnf:ro
    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci
      - --default-authentication-plugin=mysql_native_password
    networks:
      - lottery-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-proot123456"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  # 爬虫服务
  crawler-service:
    build: .
    container_name: lottery-crawler-compose
    restart: unless-stopped
    ports:
      - "4000:4000"
    environment:
      - NODE_ENV=production
      - TZ=Asia/Shanghai
      # 数据库配置
      - DB_HOST=mysql
      - DB_PORT=3306
      - DB_USER=lottery
      - DB_PASSWORD=lottery123
      - DB_NAME=lottery_crawler
      # 爬虫配置
      - CRAWLER_PORT=4000
      - CRAWLER_INTERVAL=300000
      - MAX_RETRIES=3
      # 调度器模式：continuous(持续轮询) / dynamic(倒计时) / fixed(固定间隔)
      - SCHEDULER_MODE=continuous
    volumes:
      - ./src:/app/src
      - ./logs:/app/logs
      - ./config:/app/config
      - ./data:/app/data
    depends_on:
      mysql:
        condition: service_healthy
    networks:
      - lottery-network
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

networks:
  lottery-network:
    driver: bridge

volumes:
  mysql-data:
    driver: local
```

### 3. 首次启动

```bash
# 启动所有服务
docker-compose up -d

# 等待MySQL初始化（约30秒）
docker-compose logs -f mysql

# 看到 "Database initialized" 后，检查爬虫服务
docker-compose logs -f crawler-service

# 验证数据库表已创建
docker exec lottery-mysql-compose mysql -ulottery -plottery123 lottery_crawler -e "SHOW TABLES;"
```

应该看到10个表：
- lottery_results
- crawler_logs
- alert_rules
- alert_history
- alert_stats_today (视图)
- cwl_api_domains
- cwl_domain_health_logs
- cwl_domain_switch_history
- data_completion_history
- system_settings

### 4. 常用命令

```bash
# 查看运行状态
docker-compose ps

# 查看所有服务日志
docker-compose logs -f

# 查看爬虫服务日志
docker-compose logs -f crawler-service

# 查看MySQL日志
docker-compose logs -f mysql

# 重启爬虫服务
docker-compose restart crawler-service

# 重启MySQL
docker-compose restart mysql

# 停止所有服务
docker-compose down

# 停止并删除数据
docker-compose down -v

# 重新构建并启动
docker-compose up -d --build

# 进入爬虫容器
docker exec -it lottery-crawler-compose sh

# 进入MySQL容器
docker exec -it lottery-mysql-compose mysql -ulottery -plottery123 lottery_crawler
```

---

## 手动部署

### 1. 安装Node.js依赖

```bash
cd crawler-service
npm install
```

### 2. 配置MySQL数据库

```bash
# 登录MySQL
mysql -u root -p

# 创建数据库和用户
CREATE DATABASE lottery_crawler CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'lottery'@'localhost' IDENTIFIED BY 'lottery123';
GRANT ALL PRIVILEGES ON lottery_crawler.* TO 'lottery'@'localhost';
FLUSH PRIVILEGES;

# 退出MySQL
exit

# 导入初始化脚本
mysql -u lottery -plottery123 lottery_crawler < init.sql
```

### 3. 配置环境变量

创建 `.env` 文件：

```bash
# 数据库配置
DB_HOST=localhost
DB_PORT=3306
DB_USER=lottery
DB_PASSWORD=lottery123
DB_NAME=lottery_crawler

# 服务端口
CRAWLER_PORT=4000

# 日志级别
LOG_LEVEL=info

# 调度器模式
SCHEDULER_MODE=continuous
```

### 4. 启动服务

```bash
# 开发模式（自动重启）
npm run dev

# 生产模式
npm start

# 后台运行（使用PM2）
npm install -g pm2
pm2 start src/index.js --name lottery-crawler
pm2 save
pm2 startup
```

### 5. 构建前端（如果修改了Vue代码）

```bash
cd src/web/vue-app
npm install
npm run build

# 构建产物会自动输出到 src/web/dist/
```

详细前端构建说明：[前端构建与部署指南.md](./前端构建与部署指南.md)

---

## 配置说明

### 数据源配置

数据源配置存储在数据库的 `cwl_api_domains` 表中，支持：

- **多数据源**: cwl, speedylot88, sglotteries, auluckylotteries, luckysscai, luckylottoz, uklottos, hkjc, sportslottery
- **优先级**: priority字段（数字越小优先级越高）
- **自动故障转移**: 检测到失败自动切换到备用域名
- **健康检查**: 每分钟自动检查域名可用性

在Web管理界面可以：
- 查看域名健康状态
- 手动切换域名
- 查看切换历史

### 告警配置

系统预置8条告警规则（alert_rules表）：

1. **爬取失败告警**（critical）- 连续失败3次触发
2. **响应超时告警**（warning）- 响应时间>10秒触发
3. **数据缺失告警**（error）- 期号连续缺失2期触发
4. **系统异常告警**（error）- CPU/内存>90%触发（默认禁用）
5. **502/503告警**（error）- 连续5次502/503触发
6. **数据完整性告警**（warning）- 单日完整率<90%触发
7. **WebSocket异常告警**（warning）- 断开超过5分钟触发
8. **彩种长期无更新告警**（warning）- 高频彩超过10分钟无新期号触发

### 爬虫调度配置

编辑 `src/config/crawlerConfig.js`：

```javascript
module.exports = {
  // 高频彩：75秒轮询（避免与5分钟周期冲突）
  highFrequencyInterval: 75000,

  // 低频彩：5分钟轮询
  lowFrequencyInterval: 300000,

  // 数据源超时时间
  requestTimeout: 15000,

  // 重试次数
  maxRetries: 3
};
```

---

## 常见问题

### Q1: 容器启动后无法访问管理界面？

```bash
# 检查容器状态
docker-compose ps

# 查看爬虫服务日志
docker-compose logs crawler-service

# 检查端口占用
netstat -tulnp | grep 4000

# 如果4000端口被占用，修改docker-compose.yml
ports:
  - "4001:4000"  # 改为其他端口
```

### Q2: 数据库连接失败？

```bash
# 检查MySQL容器状态
docker-compose logs mysql

# 验证数据库可访问
docker exec lottery-mysql-compose mysql -ulottery -plottery123 lottery_crawler -e "SELECT 1;"

# 检查容器网络
docker network inspect crawler-service_default
```

### Q3: 前端修改后不生效？

请参考：[前端构建与部署指南.md](./前端构建与部署指南.md)

关键步骤：
1. 修改源码：`src/web/vue-app/src/views/*.vue`
2. 重新构建：`cd src/web/vue-app && npm run build`
3. 重启服务：`docker-compose restart crawler-service`
4. 清除浏览器缓存：Ctrl+Shift+R

### Q4: 如何备份数据库？

```bash
# Docker环境
docker exec lottery-mysql-compose mysqldump -ulottery -plottery123 lottery_crawler > backup.sql

# 手动环境
mysqldump -ulottery -plottery123 lottery_crawler > backup.sql

# 恢复备份
docker exec -i lottery-mysql-compose mysql -ulottery -plottery123 lottery_crawler < backup.sql
```

### Q5: 如何查看爬虫日志？

```bash
# Docker环境
docker-compose logs -f crawler-service

# 手动环境（日志文件）
tail -f logs/crawler-$(date +%Y-%m-%d).log

# Web界面
访问 http://localhost:4000 → 系统日志
```

### Q6: 如何添加新的彩种？

参考文档：
- [docs/ADD_NEW_DATASOURCE.md](./docs/ADD_NEW_DATASOURCE.md) - 添加新数据源
- [docs/SCRAPER_TEMPLATE.md](./docs/SCRAPER_TEMPLATE.md) - 爬虫开发模板

### Q7: 如何手动触发历史数据补全？

```bash
# 进入容器
docker exec -it lottery-crawler-compose sh

# 执行补全脚本
node scripts/backfill-history.js --lottery=10035 --date=2026-01-15

# 查看补全历史
docker exec lottery-mysql-compose mysql -ulottery -plottery123 lottery_crawler \
  -e "SELECT * FROM data_completion_history ORDER BY run_time DESC LIMIT 10;"
```

---

## 维护与监控

### 日常监控

1. **Web管理界面** (http://localhost:4000)
   - 实时系统状态
   - 数据源健康检查
   - 爬虫任务日志
   - 告警历史记录

2. **数据库监控**

```sql
-- 查看今日采集统计
SELECT
  lot_code,
  COUNT(*) as total,
  MIN(draw_time) as first_draw,
  MAX(draw_time) as last_draw
FROM lottery_results
WHERE DATE(created_at) = CURDATE()
GROUP BY lot_code;

-- 查看今日告警统计
SELECT * FROM alert_stats_today;

-- 查看域名健康状态
SELECT
  source_type,
  domain_url,
  status,
  success_rate,
  response_time_ms,
  last_check_at
FROM cwl_api_domains
WHERE enabled = 1
ORDER BY source_type, priority;
```

3. **日志检查**

```bash
# Docker环境
docker-compose logs --tail=100 crawler-service

# 查看错误日志
docker-compose logs crawler-service | grep ERROR

# 查看告警日志
docker-compose logs crawler-service | grep ALERT
```

### 性能优化

1. **数据库优化**
   - 定期清理旧日志：`DELETE FROM crawler_logs WHERE created_at < DATE_SUB(NOW(), INTERVAL 30 DAY);`
   - 定期分析表：`ANALYZE TABLE lottery_results;`
   - 监控索引使用：`SHOW INDEX FROM lottery_results;`

2. **内存优化**
   - 调整MySQL内存：编辑 `my.cnf`
   - 调整Node.js堆内存：`NODE_OPTIONS="--max-old-space-size=2048"`

3. **爬虫优化**
   - 调整轮询间隔：`src/config/crawlerConfig.js`
   - 禁用不需要的彩种：在Web界面禁用
   - 优化域名优先级：在 `cwl_api_domains` 表调整

### 升级部署

```bash
# 1. 备份数据库
docker exec lottery-mysql-compose mysqldump -ulottery -plottery123 lottery_crawler > backup-$(date +%Y%m%d).sql

# 2. 拉取最新代码
git pull origin master

# 3. 重新构建并启动
docker-compose down
docker-compose up -d --build

# 4. 验证升级
docker-compose ps
docker-compose logs -f crawler-service
```

### 故障排查

常见故障：
1. **爬虫服务异常** → 查看日志，检查数据源可用性
2. **数据库连接失败** → 检查MySQL容器状态
3. **前端加载失败** → 检查dist目录是否存在，重新构建
4. **WebSocket断开** → 检查网络连接，查看服务器日志

---

## 安全建议

1. **生产环境配置**
   - 修改默认密码（MySQL root密码、lottery用户密码）
   - 使用防火墙限制3308端口访问
   - 启用HTTPS（使用Nginx反向代理）

2. **数据备份**
   - 每日自动备份数据库
   - 定期测试备份恢复
   - 异地备份存储

3. **访问控制**
   - 管理界面添加认证（建议使用Nginx BasicAuth）
   - 限制管理界面访问IP
   - 定期审查系统日志

---

## 技术支持

- **项目仓库**: https://github.com/i8jcom/kjqy-lottery-crawler
- **问题反馈**: [GitHub Issues](https://github.com/i8jcom/kjqy-lottery-crawler/issues)
- **文档**: [docs/](./docs/) 目录包含详细技术文档

---

## 相关文档

- [README.md](./README.md) - 项目概述
- [前端构建与部署指南.md](./前端构建与部署指南.md) - 前端开发部署
- [项目架构说明.md](./项目架构说明.md) - 系统架构设计
- [前端架构说明.md](./前端架构说明.md) - 前端技术架构
- [docs/ALERT_SYSTEM.md](./docs/ALERT_SYSTEM.md) - 告警系统详解
- [docs/MULTI_SOURCE_ARCHITECTURE.md](./docs/MULTI_SOURCE_ARCHITECTURE.md) - 多数据源架构
- [docs/DATABASE_OPTIMIZATION.md](./docs/DATABASE_OPTIMIZATION.md) - 数据库优化指南
