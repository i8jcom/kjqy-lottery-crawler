# 彩票爬虫系统 - GitHub推送指南

## 📅 推送时间
2026-01-17

---

## ⚠️ 重要提醒

**本次推送仅包含crawler-service（彩票爬虫系统），不包含backend**

---

## 📦 推送内容概览

### 新增文件

**核心文档** (3个):
- 前端架构说明.md (22K)
- 部署指南.md (12K)
- 项目架构说明.md (27K)

**目录整理**:
- scripts/ (脚本工具目录)
  - rebuild-and-restart.sh
  - restart-*.sh (4个重启脚本)
  - start.sh
  - migrations/
  - tests/
  - verification/
  - debugSnowball.js
  - importHKJCHistory.js
  - migrateSnowballData.js
  - 目录深度扫描.sh

- docs/ (文档目录)
  - database/ (数据库文档)
    - sql-archive/ (归档的SQL文件，13个)
    - README.md
  - maintenance/ (维护记录，9个文档)
  - archive/ (其他归档)
  - fixes/ (问题修复记录)

- data/CLAUDE.md (Claude记忆文件)

### 修改文件

- .gitignore (新增排除规则)
- init.sql (完全重写，48行→278行)

### 删除文件

**临时文件** (~80个):
- *.doc.md (文档报告，已归档)
- *.txt (测试文件)
- *.png (截图)
- *.js (临时脚本，已归档或删除)
- *.py (临时脚本)
- *.html (测试页面)
- *.sh (临时脚本，有用的已移至scripts/)
- *.md (临时文档，重要的已归档)

---

## 🚀 推送步骤

### 方法一：一次性提交所有变更（推荐）

```bash
# 1. 确认当前在crawler-service目录
cd /home/i8/claude-demo/kjqy-deploy/crawler-service

# 2. 查看待提交的变更（确认只有crawler-service的变更）
git status

# 3. 添加所有crawler-service的变更
git add .

# 4. 提交（带详细说明）
git commit -m "refactor: 完整项目重构与文档完善

### 重大更新

1. 数据库架构升级
   - 重写init.sql: 2表→10表，48行→278行
   - lottery_results: 8字段→13字段（新增金多宝识别）
   - 新增告警系统: alert_rules, alert_history, alert_stats_today
   - 新增域名管理: cwl_api_domains, cwl_domain_health_logs, cwl_domain_switch_history
   - 新增业务表: data_completion_history, system_settings
   - 预置8条告警规则和9个数据源配置

2. 项目结构重组
   - 根目录清理: 38文件→13文件
   - 新增scripts/目录: 集中管理工具脚本
   - 新增docs/目录: 文档分类存储
   - SQL文件归档: docs/database/sql-archive/
   - 维护记录归档: docs/maintenance/

3. 文档体系完善
   - 《部署指南.md》(12K): Docker/手动部署详解
   - 《项目架构说明.md》(27K): 后端架构深度解析
   - 《前端架构说明.md》(22K): Vue3+ElementPlus架构说明
   - 保留原有《前端构建与部署指南.md》

4. 前端优化
   - 删除测试文件: alert-debug.html, test-alerts-data.html
   - 修复路由: 移除11个引用deleted views-old的路由
   - 重新构建: src/web/dist/

5. 开发工具增强
   - scripts/rebuild-and-restart.sh: 一键重建前端并重启
   - scripts/migrations/: 数据库迁移脚本
   - scripts/verification/: 验证脚本

6. 安全改进
   - .gitignore更新: 排除构建产物、测试文件、日志
   - Git远程URL修复: 移除暴露的访问令牌

### Breaking Changes
- 需要使用新版init.sql重新初始化数据库（已有数据库不受影响）
- 前端需要npm run build重新构建

### 数据库迁移
对于已运行的系统：
- init.sql使用IF NOT EXISTS，不会影响现有表
- 新字段需手动添加或使用迁移脚本
- 详见docs/database/README.md

### 文档指南
新用户建议阅读顺序：
1. README.md
2. 部署指南.md
3. 项目架构说明.md
4. 前端架构说明.md
5. docs/下的专题文档
"

# 5. 推送到GitHub
git push origin main
```

### 方法二：分步提交（更清晰的历史记录）

```bash
cd /home/i8/claude-demo/kjqy-deploy/crawler-service

# 提交1：数据库升级
git add init.sql docs/database/
git commit -m "feat(db): 升级数据库架构至完整版10表

- 重写init.sql (48行→278行)
- lottery_results新增5个金多宝字段
- 新增告警系统（3个表+1个视图）
- 新增域名管理（3个表）
- 新增业务表（2个表）
- 预置8条告警规则和9个数据源
"

# 提交2：项目结构重组
git add scripts/ docs/maintenance/ docs/archive/ docs/fixes/
git commit -m "refactor: 重组项目结构

- 脚本移至scripts/目录
- 维护记录移至docs/maintenance/
- 删除80+临时文件
"

# 提交3：文档完善
git add 前端架构说明.md 部署指南.md 项目架构说明.md
git commit -m "docs: 新增核心架构文档

- 部署指南.md (12K): Docker/手动部署
- 项目架构说明.md (27K): 后端架构详解
- 前端架构说明.md (22K): Vue3架构说明
"

# 提交4：配置更新
git add .gitignore data/CLAUDE.md
git commit -m "chore: 更新配置和记忆文件

- .gitignore: 新增构建产物排除规则
- data/CLAUDE.md: 更新记忆索引
"

# 提交5：清理临时文件
git add -A
git commit -m "chore: 清理临时文件

- 删除80+临时测试文件
- 删除历史文档报告
- 删除废弃脚本
"

# 推送所有提交
git push origin main
```

---

## ✅ 推送前检查

执行以下命令确认：

```bash
# 1. 确认不包含backend变更
git status | grep backend
# 应该没有输出

# 2. 确认主要新增文件
git status --short | grep "^??" | grep -E "(前端架构说明|部署指南|项目架构说明|scripts|docs)"
# 应该看到新增的文档和目录

# 3. 确认修改的文件
git status --short | grep "^ M"
# 应该只看到.gitignore

# 4. 确认删除的文件
git status --short | grep "^ D" | wc -l
# 应该有80+个删除

# 5. 查看完整状态
git status
```

---

## 📊 推送后验证

### 1. GitHub仓库检查

访问 https://github.com/i8jcom/kjqy-lottery-crawler

检查项：
- [x] 根目录文件数约13个（配置+文档）
- [x] scripts/目录存在
- [x] docs/目录存在
- [x] 三个核心文档存在（前端架构说明.md、部署指南.md、项目架构说明.md）
- [x] README.md显示正常
- [x] init.sql是新版（278行）

### 2. 克隆测试

```bash
# 克隆到新目录
cd /tmp
git clone https://github.com/i8jcom/kjqy-lottery-crawler.git test-deploy
cd test-deploy

# 检查结构
ls -la
ls -la scripts/
ls -la docs/

# 查看init.sql
wc -l init.sql  # 应该是278行
head -20 init.sql

# 检查文档
ls -lh *.md
```

### 3. 部署测试

```bash
cd test-deploy

# Docker部署测试
docker-compose up -d

# 等待30秒（MySQL初始化）
sleep 30

# 检查容器
docker-compose ps

# 检查日志
docker-compose logs lottery-crawler | tail -50

# 检查数据库
docker exec lottery-mysql-compose mysql -ulottery -plottery123456 lottery_crawler -e "SHOW TABLES;"
# 应该看到10个表

# 检查Web界面
curl -s http://localhost:4000 | grep "彩票爬虫系统"

# 清理测试环境
docker-compose down -v
```

---

## 🔒 安全提醒

### Git令牌已移除

之前的Git远程URL包含了个人访问令牌，已经修复：

**修复前** (不安全):
```
https://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@github.com/i8jcom/kjqy-lottery-crawler.git
```

**修复后** (安全):
```
https://github.com/i8jcom/kjqy-lottery-crawler.git
```

### 推送认证

现在推送时需要输入认证信息：
```bash
git push origin main

# 会提示输入：
Username for 'https://github.com': <你的GitHub用户名>
Password for 'https://...': <个人访问令牌或密码>
```

**如何生成个人访问令牌**:
1. 访问 https://github.com/settings/tokens
2. Generate new token (classic)
3. 选择权限：repo（仓库完全访问）
4. 生成后复制令牌（只显示一次）
5. 推送时使用令牌作为密码

---

## 📝 推送记录

### 推送信息

| 项目 | 数值 |
|------|------|
| 新增文件 | ~30个 (文档+脚本+目录) |
| 修改文件 | 2个 (.gitignore, init.sql) |
| 删除文件 | ~80个 (临时文件) |
| 新增代码行 | ~1000行 (主要是init.sql和文档) |
| 删除代码行 | ~500行 (删除的临时文件) |
| 提交大小 | ~100KB (主要是文本文件) |

### 关键变更

1. **数据库**: init.sql从48行扩展到278行
2. **文档**: 新增59K文档（3个核心文档）
3. **结构**: 根目录从38文件精简到13文件
4. **归档**: 13个SQL文件归档，9个维护记录归档

---

## 🎯 推送后的下一步

### 对于新部署用户

1. 阅读 `部署指南.md`
2. 执行 `docker-compose up -d`
3. 访问 `http://localhost:4000`
4. 查看 `项目架构说明.md` 了解系统设计

### 对于开发者

1. 阅读 `前端架构说明.md`
2. 阅读 `docs/` 下的专题文档
3. 参考 `scripts/` 下的工具脚本
4. 提交Issue或PR改进项目

### 对于维护者

1. 查看 `docs/maintenance/` 历史记录
2. 使用 `scripts/rebuild-and-restart.sh` 快速重启
3. 定期备份数据库
4. 监控告警系统

---

## ✨ 完成标记

- [x] Backend变更已恢复（不推送backend）
- [x] Crawler-service清理完成
- [x] 三个核心文档已创建
- [x] 项目结构已重组
- [x] init.sql已更新
- [x] .gitignore已更新
- [x] Git远程URL已修复
- [x] 推送指南已准备

**准备就绪！可以执行推送了！** 🚀

---

## 💡 推送建议

**推荐使用方法一（一次性提交）**，原因：
1. 本次是大规模重构，单个提交更清晰
2. 提交信息已经详细说明了所有变更
3. 避免中间状态导致的问题

**命令**:
```bash
cd /home/i8/claude-demo/kjqy-deploy/crawler-service
git add .
git commit -m "refactor: 完整项目重构与文档完善

### 重大更新

1. 数据库架构升级
   - 重写init.sql: 2表→10表，48行→278行
   - lottery_results: 8字段→13字段（新增金多宝识别）
   - 新增告警系统: alert_rules, alert_history, alert_stats_today
   - 新增域名管理: cwl_api_domains, cwl_domain_health_logs, cwl_domain_switch_history
   - 新增业务表: data_completion_history, system_settings
   - 预置8条告警规则和9个数据源配置

2. 项目结构重组
   - 根目录清理: 38文件→13文件
   - 新增scripts/目录: 集中管理工具脚本
   - 新增docs/目录: 文档分类存储
   - SQL文件归档: docs/database/sql-archive/
   - 维护记录归档: docs/maintenance/

3. 文档体系完善
   - 《部署指南.md》(12K): Docker/手动部署详解
   - 《项目架构说明.md》(27K): 后端架构深度解析
   - 《前端架构说明.md》(22K): Vue3+ElementPlus架构说明
   - 保留原有《前端构建与部署指南.md》

4. 前端优化
   - 删除测试文件: alert-debug.html, test-alerts-data.html
   - 修复路由: 移除11个引用deleted views-old的路由
   - 重新构建: src/web/dist/

5. 开发工具增强
   - scripts/rebuild-and-restart.sh: 一键重建前端并重启
   - scripts/migrations/: 数据库迁移脚本
   - scripts/verification/: 验证脚本

6. 安全改进
   - .gitignore更新: 排除构建产物、测试文件、日志
   - Git远程URL修复: 移除暴露的访问令牌

### Breaking Changes
- 需要使用新版init.sql重新初始化数据库（已有数据库不受影响）
- 前端需要npm run build重新构建

### 数据库迁移
对于已运行的系统：
- init.sql使用IF NOT EXISTS，不会影响现有表
- 新字段需手动添加或使用迁移脚本
- 详见docs/database/README.md

### 文档指南
新用户建议阅读顺序：
1. README.md
2. 部署指南.md
3. 项目架构说明.md
4. 前端架构说明.md
5. docs/下的专题文档
"

git push origin main
```
